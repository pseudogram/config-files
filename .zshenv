# zshrc is executed each time you open a new terminal window, while zshenv is executed only once when you log in to your account
# the equivilant for bash is .bashrc and .bash_profile

# . is the same as source
